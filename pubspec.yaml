name: bip329_wallet_labels
description: A Dart package for managing wallet labels in compliance with BIP-329.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  freezed_annotation: ^3.0.0
  http: ^1.4.0
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^3.0.6
  get_it: ^8.0.3
  json_serializable: ^6.9.5
  lints: ^5.0.0
  test: ^1.24.0
