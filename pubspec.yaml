name: bip329_wallet_labels
description: A Dart package for managing wallet labels in compliance with BIP-329.
version: 1.0.1
repository: https://github.com/kumulynja/bip329_wallet_labels

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  http: ^1.4.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
  dotenv: ^4.2.0
